**Projet Compilation**
Réalisé par Pierre Prevost--Avenel et Jean-Matthieu Colibeau

**Utilisation**
Exécuter `clisp test.lisp` pour que le code situé dans `code.lisp` soit compilé vers `cible.asm` puis exécuté.